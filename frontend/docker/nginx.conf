server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        # Resolve at request time so the same image can start in Fargate,
        # where the ALB routes /api directly and no "backend" Docker hostname exists.
        resolver 127.0.0.11 valid=10s;
        set $backend_upstream backend:3100;
        # Sin URI al final a proposito: con una variable en proxy_pass, nginx
        # manda la URI literal ("/") en vez de la ruta original y todo
        # /api/... llegaba al backend como "/". El backend ya acepta /api/.
        proxy_pass http://$backend_upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        client_max_body_size 10m;
    }

    location / {
        try_files $uri /index.html;
    }
}
